import { useConfig } from './configs';
import { useInterceptors } from './interceptors';
import { axios, http } from './http';

export {
  axios,
  http,
  useConfig,
  useInterceptors,
};

export default http;

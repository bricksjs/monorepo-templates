// es规范
const esRules = require('./esRules');

// vue规范
const vueRules = require('./vueRules');

// 样式规范
const styleRules = require('./styleRules');

// 提交规范
const commitRules = require('./commitRules');

// 导出规则
module.exports = {
  esRules,
  vueRules,
  styleRules,
  commitRules,
};
